<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:CsArchViewer.Avalonia.Views"
        xmlns:vm="clr-namespace:CsArchViewer.Avalonia.ViewModels"
        xmlns:core="clr-namespace:CsArchViewer.Core.Models;assembly=CsArchViewer.Core"
        xmlns:symexp="clr-namespace:CsArchViewer.DotNet.SymbolExplorer.Models;assembly=CsArchViewer.DotNet.SymbolExplorer"
        mc:Ignorable="d"
        d:DesignWidth="1400"
        d:DesignHeight="820"
        x:Class="CsArchViewer.Avalonia.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="CsArchViewer - .NET Architecture Viewer"
        Icon="/Assets/Icons/logo-mark.png">
  <DockPanel>
    <TabControl Margin="0"
                Padding="0"
                TabStripPlacement="Top"
                Background="#0B1220">
      <TabItem Header="{Binding WorkspaceTabText}" FontWeight="SemiBold" Padding="14,8">
        <DockPanel>
          <Border DockPanel.Dock="Top"
                  Padding="10,8"
                  BorderBrush="#2E3748"
                  BorderThickness="0,0,0,1"
                  Background="#0B1220">
            <DockPanel LastChildFill="True">
              <!-- Top-edge loading indicator line -->
              <ProgressBar DockPanel.Dock="Top"
                           Height="4"
                           Margin="0,-8,0,6"
                           IsIndeterminate="True"
                           IsVisible="{Binding IsAnalyzing}"
                           Foreground="#60A5FA"
                           HorizontalAlignment="Stretch" />

              <WrapPanel DockPanel.Dock="Top"
                         Orientation="Horizontal"
                         VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,6,0,6">
                  <Button Content="{Binding OpenFolderText}" Click="OpenFolder_OnClick" />
                  <Button Content="{Binding ReloadText}" Click="Reload_OnClick" />
                  <Separator Width="1" Height="22" Margin="4,0" Background="#2E3748" />
                  <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                    <ComboBox x:Name="ExportFormatCombo"
                              Width="150"
                              SelectedIndex="0"
                              SelectionChanged="ExportFormatCombo_OnSelectionChanged">
                      <ComboBoxItem Content="Mermaid" />
                      <ComboBoxItem Content="JSON" />
                      <ComboBoxItem Content="DOT" />
                      <ComboBoxItem Content="AI Prompt" />
                      <ComboBoxItem Content="Metrics JSON" />
                      <ComboBoxItem Content="Metrics CSV" />
                      <ComboBoxItem Content="Metrics Markdown" />
                    </ComboBox>
                    <Button x:Name="ExportActionButton"
                            Content="{Binding ExportText}"
                            Click="Export_OnClick" />
                  </StackPanel>
                  <Separator Width="1" Height="22" Margin="4,0" Background="#2E3748" />
                  <ComboBox Width="190"
                            ItemsSource="{Binding AvailableGraphTypes}"
                            SelectedItem="{Binding SelectedGraphType, Mode=TwoWay}" />
                  <ComboBox Width="130"
                            ItemsSource="{Binding AvailableTypeFilters}"
                            SelectedItem="{Binding SelectedTypeFilter, Mode=TwoWay}" />
                  <Separator Width="1" Height="22" Margin="4,0" Background="#2E3748" />
                  <TextBox Width="280"
                           VerticalAlignment="Center"
                           PlaceholderText="{Binding SearchPlaceholderText}"
                           Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
              </WrapPanel>

              <TextBlock DockPanel.Dock="Top"
                         Margin="0,4,0,0"
                         Foreground="#94A3B8"
                         FontSize="12"
                         Text="{Binding TopStatus}"
                         TextWrapping="Wrap" />
            </DockPanel>
          </Border>

          <Grid x:Name="DiagnosticsLayoutGrid" MinHeight="200">
            <Grid.RowDefinitions>
              <RowDefinition Height="*" />
              <RowDefinition Height="6" />
              <RowDefinition Height="180" />
            </Grid.RowDefinitions>
          <Grid Grid.Row="0"
                ColumnDefinitions="260,6,*,6,300"
                MinHeight="120">
            <Border Grid.Column="0"
                    MinWidth="160"
                    BorderBrush="#2E3748"
                    BorderThickness="0,0,1,0">
              <TabControl Margin="4">
                <TabItem Header="{Binding NodesTitleText}" FontWeight="SemiBold">
                  <DockPanel LastChildFill="True">
                    <ListBox Margin="6"
                             ItemsSource="{Binding ListedNodes}"
                             SelectedItem="{Binding SelectedListedNode, Mode=TwoWay}">
                      <ListBox.ItemTemplate>
                        <DataTemplate x:DataType="core:ArchitectureNode">
                          <StackPanel Margin="4">
                            <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                            <TextBlock FontSize="11" Foreground="#94A3B8" Text="{Binding Type}" />
                          </StackPanel>
                        </DataTemplate>
                      </ListBox.ItemTemplate>
                    </ListBox>
                  </DockPanel>
                </TabItem>
                <TabItem Header="{Binding SymbolExplorerTabText}" FontWeight="SemiBold">
                  <DockPanel LastChildFill="True">
                    <DockPanel DockPanel.Dock="Top"
                               Margin="8,8,8,4"
                               LastChildFill="True">
                      <Button DockPanel.Dock="Right"
                              Margin="8,0,0,0"
                              Padding="10,4"
                              Content="{Binding SymbolExplorerSearchButtonText}"
                              Click="SymbolExplorerSearch_OnClick" />
                      <TextBox VerticalAlignment="Center"
                               PlaceholderText="{Binding SymbolExplorerSearchPlaceholderText}"
                               Text="{Binding SymbolExplorerSearchQuery}" />
                    </DockPanel>
                    <ListBox Margin="8,0,8,8"
                             ItemsSource="{Binding SymbolExplorerResults}"
                             SelectedItem="{Binding SelectedExplorerSymbol, Mode=TwoWay}"
                             SelectionChanged="SymbolExplorerResults_OnSelectionChanged">
                      <ListBox.ItemTemplate>
                        <DataTemplate x:DataType="symexp:SymbolInfoModel">
                          <StackPanel Margin="2">
                            <TextBlock FontWeight="SemiBold" Text="{Binding DisplayName}" />
                            <TextBlock FontSize="11" Foreground="#94A3B8" Text="{Binding Kind}" />
                            <TextBlock FontSize="10" Foreground="#64748B" Text="{Binding Namespace}" TextTrimming="CharacterEllipsis" />
                          </StackPanel>
                        </DataTemplate>
                      </ListBox.ItemTemplate>
                    </ListBox>
                  </DockPanel>
                </TabItem>
              </TabControl>
            </Border>

            <GridSplitter Grid.Column="1"
                          Width="6"
                          Background="#1E293B"
                          ResizeDirection="Columns" />

            <Border Grid.Column="2" MinWidth="200" Background="#111827">
              <Grid RowDefinitions="Auto,*">
                <Border Background="#0F172A"
                        BorderBrush="#1E293B"
                        BorderThickness="0,0,0,1"
                        Padding="8">
                  <WrapPanel Orientation="Horizontal">
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#1F2937" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Library" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#065F46" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Exe / WinExe" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#4C1D95" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Folder" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#334155" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="File" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#0EA5E9" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="XAML/AXAML File" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#8B5CF6" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Razor File" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#F97316" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="CSHTML File" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#7C2D12" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Solution" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#14B8A6" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Package" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#0F766E" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Namespace" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#B91C1C" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Violation" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#1D4ED8" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Type" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#38BDF8" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Interface" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#7C3AED" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Struct" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#BE185D" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Enum" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14" Background="#10B981" CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Record" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,2,16,6">
                      <Border Width="14" Height="14"
                              BorderBrush="#F59E0B"
                              BorderThickness="2"
                              CornerRadius="3" />
                      <TextBlock VerticalAlignment="Center" Foreground="#CBD5E1" Text="Search Hit" />
                    </StackPanel>
                  </WrapPanel>
                </Border>
                <views:GraphView x:Name="GraphViewControl"
                                 Grid.Row="1"
                                 DataContext="{Binding Graph}" />
              </Grid>
            </Border>

            <GridSplitter Grid.Column="3"
                          Width="6"
                          Background="#1E293B"
                          ResizeDirection="Columns" />

            <Border Grid.Column="4"
                    MinWidth="200"
                    BorderBrush="#2E3748"
                    BorderThickness="1,0,0,0">
              <TabControl>
                <TabItem Header="{Binding NodeDetailsTitleText}" FontWeight="SemiBold">
                  <ScrollViewer>
                    <StackPanel Margin="10" Spacing="8">
                  <Expander Header="基本資訊"
                            IsExpanded="True"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch">
                    <StackPanel Spacing="0" HorizontalAlignment="Stretch">
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding ProjectNameText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.ProjectName}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding NodeTypeText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.NodeType}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding FullPathText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.FullPath}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding ExtensionText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.Extension}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding LastModifiedText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.LastModified}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                    </StackPanel>
                  </Expander>

                  <Expander Header="專案/套件"
                            IsExpanded="False"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch">
                    <StackPanel Spacing="0" HorizontalAlignment="Stretch">
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding TargetFrameworkText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.TargetFramework}" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding OutputTypeText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.OutputType}" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding ProjectReferencesText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.ProjectReferences}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding PackageReferencesText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.PackageReferences}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                    </StackPanel>
                  </Expander>

                  <Expander Header="Code Metrics"
                            IsExpanded="False"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch">
                    <StackPanel Spacing="0" HorizontalAlignment="Stretch">
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding SizeBytesText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.Size}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding ChildCountText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.ChildCount}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding LineCountText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.LineCount}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                    </StackPanel>
                  </Expander>

                  <Expander Header="相依與規則"
                            IsExpanded="False"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch">
                    <StackPanel Spacing="0" HorizontalAlignment="Stretch">
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding DependencyCountText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.DependencyCount}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding CircularDependencyCountText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.CircularDependencyCount}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding ViolationCountText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.ViolationCount}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding DependsOnText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.DependsOn}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding IncomingDependenciesText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.DependencyIncoming}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding RuleText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.Rule}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding MessageText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.Message}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                    </StackPanel>
                  </Expander>

                  <Expander Header="型別資訊"
                            IsExpanded="False"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch">
                    <StackPanel Spacing="0" HorizontalAlignment="Stretch">
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding FullTypeNameText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.FullTypeName}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding NamespaceText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.NamespaceName}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding FileText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.File}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding BaseTypeText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.BaseType}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding ImplementedInterfacesText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.ImplementedInterfaces}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding ReferencedTypesText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.ReferencedTypes}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding ReferencedNamespacesText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.ReferencedNamespaces}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding ReferencedByText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.ReferencedBy}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding SourceText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.Source}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding TargetText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.Target}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                      <Border BorderBrush="#2E3748" BorderThickness="0,0,0,1" Padding="0,6">
                        <StackPanel Spacing="2">
                          <TextBlock Text="{Binding DependencyMatrixRowText}" FontWeight="SemiBold" />
                          <TextBlock Text="{Binding NodeDetails.MatrixRow}" TextWrapping="Wrap" />
                        </StackPanel>
                      </Border>
                    </StackPanel>
                  </Expander>
                </StackPanel>
                  </ScrollViewer>
                </TabItem>
                <TabItem Header="{Binding SymbolExplorerDetailsTabText}" FontWeight="SemiBold">
                  <ScrollViewer>
                    <StackPanel Margin="10" Spacing="8">
                      <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="{Binding SymbolExplorerFindRefsText}"
                                Click="SymbolExplorerFindRefs_OnClick" />
                        <Button Content="{Binding SymbolExplorerGoToDefText}"
                                Click="SymbolExplorerGoToDef_OnClick" />
                      </StackPanel>
                      <TextBlock Text="{Binding ExplorerSymbolDetailsText}"
                                 TextWrapping="Wrap"
                                 Foreground="#CBD5E1" />
                      <Separator Background="#2E3748" />
                      <TextBlock FontWeight="Bold"
                                 Text="{Binding SymbolExplorerTypeMembersTitleText}" />
                      <TextBlock Text="{Binding ExplorerTypeMembersSummary}"
                                 TextWrapping="Wrap"
                                 FontSize="11"
                                 Foreground="#94A3B8" />
                      <ListBox Height="220"
                               ItemsSource="{Binding ExplorerTypeMethods}"
                               SelectedItem="{Binding SelectedExplorerMethod, Mode=TwoWay}"
                               SelectionChanged="ExplorerMethods_OnSelectionChanged">
                        <ListBox.ItemTemplate>
                          <DataTemplate x:DataType="symexp:MethodInfoModel">
                            <StackPanel Margin="2">
                              <TextBlock FontWeight="SemiBold"
                                         Text="{Binding Signature}"
                                         TextWrapping="Wrap" />
                              <TextBlock FontSize="10"
                                         Foreground="#64748B"
                                         Text="{Binding FilePath}"
                                         TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                          </DataTemplate>
                        </ListBox.ItemTemplate>
                      </ListBox>
                      <TextBlock FontWeight="Bold"
                                 Text="{Binding SymbolExplorerMethodMetaTitleText}" />
                      <TextBlock Text="{Binding ExplorerMethodMetadataText}"
                                 TextWrapping="Wrap"
                                 FontFamily="Consolas"
                                 FontSize="11" />
                    </StackPanel>
                  </ScrollViewer>
                </TabItem>
              </TabControl>
            </Border>
          </Grid>

          <GridSplitter Grid.Row="1"
                        x:Name="DiagnosticsSplitter"
                        Height="6"
                        DoubleTapped="DiagnosticsSplitter_OnDoubleTapped"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Background="#1E293B"
                        ResizeDirection="Rows" />

          <Border Grid.Row="2"
                  BorderBrush="#2E3748"
                  BorderThickness="0,1,0,0"
                  Padding="8"
                  Background="#0B1220">
            <TabControl>
              <TabItem Header="{Binding BottomPanelDiagnosticsTabText}" FontWeight="SemiBold">
            <Grid ColumnDefinitions="2*,6,*" RowDefinitions="Auto,*">
              <DockPanel LastChildFill="False">
                <Button x:Name="DiagnosticsToggleButton"
                        DockPanel.Dock="Left"
                        Width="24"
                        Height="20"
                        Padding="0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Content="▾"
                        Click="DiagnosticsToggleButton_OnClick" />
                <TextBlock DockPanel.Dock="Left"
                           Margin="6,0,0,0"
                           Text="{Binding DiagnosticsTitleText}"
                           FontWeight="Bold" />
              </DockPanel>
              <ListBox Grid.Row="1"
                       Margin="0,8,0,0"
                       ItemsSource="{Binding Diagnostics}">
                <ListBox.ItemTemplate>
                  <DataTemplate>
                    <StackPanel Spacing="2" Margin="2">
                      <TextBlock Text="{Binding Severity}" FontWeight="SemiBold" />
                      <TextBlock Text="{Binding Type}" />
                      <TextBlock Text="{Binding Source}" TextWrapping="Wrap" Foreground="#94A3B8" />
                      <TextBlock Text="{Binding Target}" TextWrapping="Wrap" Foreground="#64748B" />
                      <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
                    </StackPanel>
                  </DataTemplate>
                </ListBox.ItemTemplate>
              </ListBox>

              <GridSplitter Grid.Column="1"
                            Grid.RowSpan="2"
                            Width="6"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="#1E293B"
                            ResizeDirection="Columns" />

              <TextBlock Grid.Column="2"
                         Text="{Binding TopFilesByLineCountText}"
                         FontWeight="Bold"
                         Margin="12,0,0,0" />
              <ListBox Grid.Row="1"
                       Grid.Column="2"
                       Margin="12,8,0,0"
                       ItemsSource="{Binding TopFilesByLineCount}">
                <ListBox.ItemTemplate>
                  <DataTemplate>
                    <StackPanel Spacing="2" Margin="2">
                      <TextBlock FontWeight="SemiBold"
                                 Text="{Binding FileName}" />
                      <TextBlock Foreground="#94A3B8"
                                 Text="{Binding FilePath}"
                                 TextWrapping="NoWrap"
                                 TextTrimming="CharacterEllipsis" />
                      <TextBlock Foreground="#CBD5E1"
                                 Text="{Binding LineCount, StringFormat={}{0} lines}" />
                    </StackPanel>
                  </DataTemplate>
                </ListBox.ItemTemplate>
              </ListBox>
            </Grid>
              </TabItem>
              <TabItem Header="{Binding SymbolExplorerReferencesTabText}" FontWeight="SemiBold">
                <DockPanel LastChildFill="True">
                  <StackPanel DockPanel.Dock="Top"
                              Orientation="Horizontal"
                              Spacing="8"
                              Margin="0,0,0,8">
                    <Button Content="{Binding SymbolExplorerJumpOpenText}"
                            Click="ExplorerReferencesOpen_OnClick" />
                  </StackPanel>
                  <ListBox ItemsSource="{Binding ExplorerReferences}"
                           SelectedItem="{Binding SelectedExplorerReference, Mode=TwoWay}"
                           DoubleTapped="ExplorerReferences_OnDoubleTapped">
                    <ListBox.ItemTemplate>
                      <DataTemplate x:DataType="symexp:ReferenceInfoModel">
                        <StackPanel Margin="2" Spacing="2">
                          <TextBlock FontWeight="SemiBold"
                                     Text="{Binding ReferencingSymbolDisplay}"
                                     TextWrapping="Wrap" />
                          <TextBlock FontSize="11"
                                     Foreground="#94A3B8"
                                     Text="{Binding FilePath}"
                                     TextTrimming="CharacterEllipsis" />
                          <TextBlock FontSize="11"
                                     Foreground="#64748B"
                                     Text="{Binding LineNumber, StringFormat=Line {0}}" />
                        </StackPanel>
                      </DataTemplate>
                    </ListBox.ItemTemplate>
                  </ListBox>
                </DockPanel>
              </TabItem>
            </TabControl>
          </Border>
        </Grid>
        </DockPanel>
      </TabItem>
      <TabItem Header="{Binding MetricsTabText}" FontWeight="SemiBold" Padding="14,8">
        <ScrollViewer>
          <StackPanel Margin="20" Spacing="16">
            <TextBlock Text="{Binding MetricsTabText}" FontSize="22" FontWeight="SemiBold" />
            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto">
              <TextBlock Grid.Column="0" Text="{Binding TotalLocText}" FontWeight="SemiBold" />
              <TextBlock Grid.Column="1" Text="{Binding MetricsTotalLoc}" />
              <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding TotalFilesText}" FontWeight="SemiBold" />
              <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding MetricsTotalFiles}" />
              <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding LargestFileText}" FontWeight="SemiBold" />
              <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding MetricsLargestFile}" TextWrapping="Wrap" />
            </Grid>
            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto">
              <TextBlock Grid.Column="0" Text="{Binding LargestNamespaceText}" FontWeight="SemiBold" />
              <TextBlock Grid.Column="1" Text="{Binding MetricsLargestNamespace}" TextWrapping="Wrap" />
              <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding CircularDependenciesText}" FontWeight="SemiBold" />
              <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding MetricsCircularDependencies}" />
              <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding LayerViolationsText}" FontWeight="SemiBold" />
              <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding MetricsLayerViolations}" />
            </Grid>

            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,*">
              <TextBlock Grid.Column="0" Text="{Binding TopCoupledNamespacesText}" FontWeight="Bold" />
              <TextBlock Grid.Column="1" Text="{Binding HealthWarningsText}" FontWeight="Bold" Margin="12,0,0,0" />
              <ListBox Grid.Row="1" Grid.Column="0" Margin="0,8,6,0" ItemsSource="{Binding TopCoupledNamespaces}">
                <ListBox.ItemTemplate>
                  <DataTemplate>
                    <StackPanel Margin="2">
                      <TextBlock Text="{Binding Namespace}" FontWeight="SemiBold" />
                      <TextBlock Text="{Binding TotalLines, StringFormat={}{0} lines}" />
                      <TextBlock Text="{Binding DependencyCount, StringFormat=dep: {0}}" />
                    </StackPanel>
                  </DataTemplate>
                </ListBox.ItemTemplate>
              </ListBox>
              <ListBox Grid.Row="1" Grid.Column="1" Margin="6,8,0,0" ItemsSource="{Binding HealthWarnings}">
                <ListBox.ItemTemplate>
                  <DataTemplate>
                    <StackPanel Margin="2">
                      <TextBlock Text="{Binding Type}" FontWeight="SemiBold" />
                      <TextBlock Text="{Binding Source}" Foreground="#94A3B8" TextWrapping="Wrap" />
                      <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
                    </StackPanel>
                  </DataTemplate>
                </ListBox.ItemTemplate>
              </ListBox>
            </Grid>
          </StackPanel>
        </ScrollViewer>
      </TabItem>
      <TabItem Header="{Binding SettingsTabText}" FontWeight="SemiBold" Padding="14,8">
        <views:SettingsView />
      </TabItem>
    </TabControl>
  </DockPanel>
</Window>
